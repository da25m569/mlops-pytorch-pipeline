apiVersion: apps/v1
kind: Deployment
metadata:
  name: model-serving
  namespace: ml-training
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: model-serving
  template:
    metadata:
      labels:
        app: model-serving
    spec:
      containers:
        - name: serving
          image: mlops-cifar-serve:v1
          imagePullPolicy: IfNotPresent
          env:
            - name: MODEL_PATH
              value: /app/checkpoints/classifier_v1.pt
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "1"
              memory: "2Gi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            periodSeconds: 5
            initialDelaySeconds: 15
          volumeMounts:
            - name: checkpoints
              mountPath: /app/checkpoints
              readOnly: true
      volumes:
        - name: checkpoints
          persistentVolumeClaim:
            claimName: cifar10-checkpoints-pvc
